# .env Confession Booth

A brutally honest .env file analyzer that finds unused variables, weak secrets, and naming sins. Run: `python env_confession.py` in your project root. Your secrets will be judged.